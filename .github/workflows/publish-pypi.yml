name: Publish Python package

on:
  release:
    types: [published]

jobs:
  build-and-publish:
    name: Build and publish to PyPI
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          # Ensure we build from the exact release tag
          ref: ${{ github.event.release.tag_name }}
          # Fetch full history so setuptools_scm or similar tools (if added later)
          # and version comparisons work correctly
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip build twine

      - name: Build distribution
        run: |
          python -m build --sdist --wheel

      - name: Publish to PyPI
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}
        run: |
          # ensure twine is available in the runner environment
          python -m pip install --upgrade twine
          # upload built distributions
          python -m twine upload dist/*
